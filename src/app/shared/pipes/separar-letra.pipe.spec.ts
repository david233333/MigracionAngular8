import { SepararLetraPipe } from './separar-letra.pipe';

describe('SepararLetraPipe', () => {
  it('create an instance', () => {
    const pipe = new SepararLetraPipe();
    expect(pipe).toBeTruthy();
  });
});
